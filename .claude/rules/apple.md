---
paths:
  - "apps/apple/**"
---

# Apple app rules

You are touching `apps/apple/**`. Read `apps/apple/AGENTS.md` before editing — it is the
authoritative area contract (location authorization, the offline contract, architecture
rules, Swift 6 quality gates). The root `AGENTS.md` still applies.

Validate with `make apple-validate`. Full workflow: `docs/AGENT_WORKFLOWS.md`.
